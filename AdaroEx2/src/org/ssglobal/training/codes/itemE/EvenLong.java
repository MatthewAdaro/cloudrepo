package org.ssglobal.training.codes.itemE;

public class EvenLong {
	
	/* Return true if the number x is even, false otherwise
	 */
	public static boolean even(long x) {
		return (x % 2 == 0) ? true : false;
	}
}
